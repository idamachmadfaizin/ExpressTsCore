export declare class ResponseLog {
    method: string;
    pathname: string;
    statusCode: number;
    message: string;
    constructor(method: string, pathname: string, statusCode: number, message: string);
}
